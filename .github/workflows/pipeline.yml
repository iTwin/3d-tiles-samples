name: Build and lint

on:
  workflow_dispatch:
  push:
    branches:
    - 'main'
  pull_request:

jobs:
  build_cesium_sample:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install
        working-directory: ./cesium-viewer-sample

      - run: npm run build
        working-directory: ./cesium-viewer-sample

      - run: npm run lint
        working-directory: ./cesium-viewer-sample

  build_three_sample:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install
        working-directory: ./three-viewer-sample

      - run: npm run build
        working-directory: ./three-viewer-sample

      - run: npm run lint
        working-directory: ./three-viewer-sample